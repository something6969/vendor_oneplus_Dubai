# Android fstab file.
#<src>                          <mnt_point>         <type>              <mnt_flags and options>                 <fs_mgr_flags>
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK
/dev/block/platform/mstar_mci.0/by-name/3rd_rw         /data/vendor/3rd_rw  ext4      defaults,noatime,nosuid,nodev,noexec                  wait
/dev/block/platform/mstar_mci.0/by-name/upgrade        /data/vendor/upgrade ext4      defaults,noatime,nosuid,nodev,noexec                  wait
